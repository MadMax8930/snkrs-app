import { HomeMain } from '@/components';

export default function Home() {
  return (
    <main className="flex min-h-full bg-white flex-col items-center justify-between">
      <HomeMain />
    </main>
  )
}
